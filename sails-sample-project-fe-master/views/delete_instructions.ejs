<ol class="breadcrumb">
    <li><a href="/">Home</a></li>
    <li class="active">Delete Instructions</a></li>
</ol>

<br/>
<%- partial ('partials/statusmessage.ejs') %>


<!-- //Adding instructions form -->
<h2> Delete Instructions</h2>
<select id="recipe_instruction_destroyer" name="recipe_id">
      <% if(typeof recipes != "undefined" && recipes.length > 0){ %>
        <% recipes.forEach(function(recipe){ %>
          <option value="<%= encodeURI(JSON.stringify(recipe)) %>">
            <%= recipe.title %>
          </option>
        <% }) %>
      <% } %>
      </select>

      <div>

      <table>
      <thead>
      </thead>
      <tbody id="deleteInstructionsForm">
      </tbody>
      </table>


      </div>
